#include<stdio.h>
int arr[3],i=0,l,r,k,c=0;
int main()
{
    while(i<3 && scanf("%d",&arr[i]) == 1)
        i++;
        k = arr[2];
        for(l = arr[0];l <arr[1]+ear
            ;l++)
        {
            if(l % k == 0 )
            {      
                c = c + 1;
            }
                else
                    continue;
        }
        printf("%d",c);

}
